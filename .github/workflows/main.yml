name: Push Docker Image  🐳🐳🐳

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3.5.2
    
    - name: Docker Login
      uses: docker/login-action@v2.1.0
      with:
       registry: harbor.gavtech.com.br
       username: humberto.crispim
       password: Saxa211193@@
    - name: Build and push Docker images
      uses: docker/build-push-action@v4.0.0
      with:
        context: ./src
        file: ./src/Dockerfile
        push: true
        tags: |
          harbor.gavtech.com.br/gavtech/kube-news:v1
        
